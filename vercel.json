{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run start",
  "installCommand": "npm install",
  "framework": "vite",
  "version": 2,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://coupon.netmarble.com/api/$1"
    }
  ]
}
